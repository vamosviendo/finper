[pytest]
DJANGO_SETTINGS_MODULE = finper.settings
python_files = tests.py test_*.py *_tests.py
markers =
    nomonbase: no carga moneda base
    slow: tests lentos, excluidos del CI Por defecto
addopts = --no-migrations -m "not slow"

